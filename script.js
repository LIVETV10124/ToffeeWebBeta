function openPlayer(playerId, source, cookie) {
    // Display modal or load player
    alert(`Opening player for ${playerId} with source: ${source}`);
    // Future implementation for a modal or new page player
}
